{
  "manifest_version": "0.2",
  "name": "snaprender",
  "version": "1.5.2",
  "description": "Screenshot, extract, and render web pages via the SnapRender API",
  "author": {
    "name": "SnapRender"
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "SNAPRENDER_API_KEY": "${user_config.api_key}"
      }
    }
  },
  "user_config": {
    "api_key": {
      "type": "string",
      "title": "SnapRender API Key",
      "description": "Your API key from app.snap-render.com/dashboard (starts with sk_live_)",
      "sensitive": true,
      "required": true
    }
  },
  "license": "MIT"
}
